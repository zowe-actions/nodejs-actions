name: "bump-version"
description: "version bump for NodeJS project"
inputs:
  base-directory:
    description: 'the base directory to run version bump'
    required: false
  manifest-directory:
    description: 'force the manifest directory when bumping version. overrules base-directory'
    required: false
  npm-package-directory:
    description: 'force the npm package directory when bumping version. overrules base-directory'
    required: false
  version:
    description: 'which version level needed to bump version, eg. npm version patch'
    required: false
    default: 'patch'
runs:
  using: 'node20'
  main: 'dist/index.js'